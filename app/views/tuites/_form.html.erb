<%= form_with model: @tuite do |form| %>
  <% if @tuite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tuite.errors.count, "error") %> prohibited this tuite from being saved:</h2>

      <ul>
        <% tuite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="control">
      <%= form.label :tuite, "No que você está pensando?", class: "label" %>
      <%= form.text_area :tuite, placeholder: "Escreva um novo tuite...", class: "textarea" %>
    </div>
  </div>

  <%= form.submit "Tuitar", class: "button is-info" %>
<% end %>

